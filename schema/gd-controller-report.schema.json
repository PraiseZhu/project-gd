{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "gd-controller-report.schema.json",
  "title": "GD Controller Report",
  "description": "Schema for the /gd controller run report produced after a full dispatch+review+gate cycle. Supports schema_version 1.0 (legacy) and 1.1 (Rev21 batch ledger + suite_target_closure).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "run_mode",
    "started_at",
    "finished_at",
    "aggregate_path",
    "manifest_path",
    "primary_gate",
    "secondary_gate",
    "gate_consistent",
    "dirty_detected",
    "jobs"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "enum": ["1.0", "1.1"],
      "description": "Schema version. '1.0' = legacy; '1.1' = Rev21 (adds batch_ledgers[] + suite_target_closure[])."
    },
    "run_mode": {
      "type": "string",
      "enum": ["live", "fixture", "mock_only"],
      "description": "Execution mode. 'fixture' / 'mock_only' runs are closure-ineligible and will be rejected by the parent close gate."
    },
    "started_at": {
      "type": "string",
      "description": "ISO 8601 timestamp when the controller run started."
    },
    "finished_at": {
      "type": "string",
      "description": "ISO 8601 timestamp when the controller run finished."
    },
    "aggregate_path": {
      "type": "string",
      "description": "Filesystem path to the cross-review aggregate JSON produced by this run."
    },
    "manifest_path": {
      "type": "string",
      "description": "Filesystem path to the dispatch manifest JSON used for this run."
    },
    "primary_gate": {
      "type": "object",
      "description": "Outcome of the primary (Claude self-review) gate.",
      "additionalProperties": false,
      "required": ["verdict", "blocking"],
      "properties": {
        "verdict": {
          "type": "string",
          "description": "Gate verdict string (e.g. APPROVED, REQUIRES_CHANGES, FAILED)."
        },
        "blocking": {
          "type": "array",
          "items": { "type": "string" },
          "description": "List of blocking finding IDs or descriptions from this gate."
        }
      }
    },
    "secondary_gate": {
      "type": "object",
      "description": "Outcome of the secondary (Codex cross-review) gate.",
      "additionalProperties": false,
      "required": ["verdict", "blocking"],
      "properties": {
        "verdict": {
          "type": "string",
          "description": "Gate verdict string (e.g. APPROVED, REQUIRES_CHANGES, FAILED)."
        },
        "blocking": {
          "type": "array",
          "items": { "type": "string" },
          "description": "List of blocking finding IDs or descriptions from this gate."
        }
      }
    },
    "gate_consistent": {
      "type": "boolean",
      "description": "True when primary and secondary gates agree on the final verdict."
    },
    "dirty_detected": {
      "type": "boolean",
      "description": "True when the controller detected uncommitted or unexpected filesystem changes during the run."
    },
    "jobs": {
      "type": "array",
      "description": "One entry per dispatched job in this controller run.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "queue_job_id",
          "target_role",
          "primary_target",
          "bridge_exit",
          "bridge_stderr_path",
          "bridge_stderr_summary",
          "raw_verdict",
          "mapped_status",
          "aggregate_bucket"
        ],
        "properties": {
          "queue_job_id": {
            "type": "string",
            "description": "Unique identifier for this job within the dispatch queue."
          },
          "target_role": {
            "type": "string",
            "description": "Role label for the review target (e.g. 'plan', 'code', 'execution')."
          },
          "primary_target": {
            "type": "string",
            "description": "Filesystem path or identifier of the primary artifact being reviewed."
          },
          "bridge_exit": {
            "type": "integer",
            "description": "Exit code returned by the Codex bridge process for this job."
          },
          "bridge_stderr_path": {
            "type": ["string", "null"],
            "description": "Path to the file containing bridge stderr output, or null if none was captured."
          },
          "bridge_stderr_summary": {
            "type": "string",
            "description": "One-line summary of bridge stderr (or 'none' / 'fixture_mode_no_real_bridge')."
          },
          "raw_verdict": {
            "type": "string",
            "description": "Raw verdict string extracted from the bridge output."
          },
          "mapped_status": {
            "type": "string",
            "description": "Normalized status after verdict mapping (e.g. approved, requires_changes, failed, degraded)."
          },
          "aggregate_bucket": {
            "type": "string",
            "description": "Bucket name used when writing this job result into the cross-review aggregate."
          }
        }
      }
    },
    "batch_ledgers": {
      "type": "array",
      "description": "v1.1: list of stage dispatch ledger files produced by this controller run, with hashes for integrity verification.",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": ["path", "hash"],
        "properties": {
          "path": {
            "type": "string",
            "description": "Filesystem path to the ledger file (relative to controller report or absolute)."
          },
          "hash": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$",
            "description": "SHA-256 hex digest of the ledger file contents."
          }
        }
      }
    },
    "suite_target_closure": {
      "type": "array",
      "description": "v1.1: closure summary for every dispatched job, mirroring jobs[] for closure-gate consumption.",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": ["target_id", "mapped_status", "evidence_kind"],
        "properties": {
          "target_id": {
            "type": "string",
            "description": "Job identifier matching jobs[].queue_job_id."
          },
          "mapped_status": {
            "type": "string",
            "description": "Normalized closure status; INELIGIBLE_STATUSES are rejected by the parent close gate."
          },
          "evidence_kind": {
            "type": "string",
            "enum": ["controller_approved", "mapped_status_approved", "n_a"],
            "description": "Provenance of the closure verdict: controller_approved (controller-level approval), mapped_status_approved (status-derived), n_a (no applicable evidence)."
          }
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "schema_version": { "const": "1.1" } }
      },
      "then": {
        "required": ["batch_ledgers", "suite_target_closure"]
      }
    }
  ]
}
